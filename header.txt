      <header className={`relative shrink-0 transition-colors duration-300 border-b z-10 ${
        isDark 
          ? "bg-zinc-950/90 border-zinc-800 text-zinc-100" 
          : "bg-white/95 backdrop-blur-md border-indigo-100 shadow-sm text-slate-900"
      }`}>
        <div className="absolute inset-0 overflow-hidden pointer-events-none">
          <div className={`absolute -top-[150px] -left-[100px] w-[400px] h-[400px] rounded-full blur-3xl opacity-20 ${isDark ? "bg-indigo-600" : "bg-indigo-300/50"}`} />
        </div>
        
        <div className="relative max-w-7xl mx-auto px-3 sm:px-6 py-2 sm:py-3">
          
          {/* Mobile Upright/Compact Layout (Only visible on screens narrower than sm) */}
          <div className="flex sm:hidden flex-col gap-2">
            {/* Row 1: Patient Info & Elapsed Time Status */}
            <div className="flex items-center justify-between gap-2 min-w-0">
              <div className="flex-1 flex items-center gap-2 min-w-0">
                <div className={`relative flex items-center justify-center w-8 h-8 rounded-full border shadow-sm shrink-0 ${
                  isDark ? "bg-zinc-900 border-zinc-700" : "bg-indigo-50/50 border-indigo-100"
                }`}>
                  <FileText className={`w-3.5 h-3.5 ${isDark ? "text-indigo-400" : "text-indigo-600"}`} />
                  <span className={`absolute top-0 right-0 w-2 h-2 rounded-full border-2 ${isDark ? "border-zinc-900" : "border-white"} ${document.timers.startAnesthesia ? "bg-emerald-500 animate-pulse" : "bg-slate-300"}`}></span>
                </div>
                
                <div className="flex flex-col min-w-0 flex-1">
                  <div className="flex items-center gap-1">
                    <h1 className="text-xs font-bold tracking-tight uppercase truncate">
                      {document.patient?.fullName || "Sem Identificação"}
                    </h1>
                    {document.status === "Signed" ? (
                      <span className="shrink-0 px-1 py-0.5 rounded text-xs font-black uppercase tracking-widest bg-indigo-500/20 text-indigo-300 border border-indigo-500/20">
                        Assinado
                      </span>
                    ) : (
                      <span className={`shrink-0 px-1 py-0.5 rounded text-xs font-black uppercase tracking-widest ${
                        isDark ? "bg-amber-500/20 text-amber-300 border border-amber-500/20" : "bg-amber-100 text-amber-700 border border-indigo-100"
                      }`}>
                        Draft
                      </span>
                    )}
                  </div>
                  <div className={`flex items-center gap-1 text-xs font-semibold truncate ${isDark ? "text-zinc-400" : "text-slate-500"}`}>
                    <span>{document.patient?.age ? `${document.patient?.age}a` : "—"}</span>
                    <span className="opacity-30">•</span>
                    <span>{document.patient?.weight ? `${document.patient?.weight}kg` : "—"}</span>
                  </div>
                </div>
              </div>

              {/* Time Status */}
              <div className={`flex shrink-0 items-center gap-1 px-2 py-1 rounded-md border shadow-xs ${
                 isDark ? "bg-zinc-900/80 border-zinc-700" : "bg-white/80 border-indigo-100/80"
              }`}>
                <div className="flex flex-col text-left">
                  <span className="text-xs font-bold uppercase tracking-wider opacity-60 leading-none">Anesth</span>
                  <span className={`text-xs font-black tabular-nums leading-none mt-0.5 ${isDark ? "text-indigo-400" : "text-indigo-600"}`}>
                    {getElapsedAnesthesiaString()}
                  </span>
                </div>
                <Clock className={`w-2.5 h-2.5 ${document.timers.startAnesthesia ? "text-emerald-500 animate-pulse" : "text-indigo-400"}`} />
              </div>
            </div>

            {/* Row 2: Permanent Sync Status & Compact Action Controls */}
            <div className="flex items-center justify-between gap-1">
              <div className="min-w-0 flex-1 flex items-center">
                <SyncStatusBadge
                  status={syncEngine.status}
                  statusText={syncEngine.statusText}
                  isOnline={syncEngine.isOnline}
                  pendingCount={syncEngine.pendingCount}
                  lastSavedAt={syncEngine.lastSavedAt}
                  errorMessage={syncEngine.errorMessage}
                  onRetry={syncEngine.retrySyncNow}
                  isDark={isDark}
                  compact
                />
              </div>

              {/* Combined compact control groups */}
              <div className="flex items-center gap-1 shrink-0">
                {/* Group A: Sheets & Data Actions */}
                <div className={`flex items-center gap-0.5 p-0.5 rounded-md border shadow-xs ${
                   isDark ? "bg-zinc-900/50 border-zinc-800" : "bg-white/50 border-indigo-50/80"
                }`}>
                  <button 
                    onClick={() => setShowShareModal(true)} 
                    disabled={!document.userId} 
                    title="Sincronizar"
                    className={`p-1 rounded transition-colors flex items-center justify-center ${
                      syncEngine.status === "syncing"
                        ? "bg-emerald-100 text-emerald-700 dark:bg-emerald-500/20 dark:text-emerald-300 animate-pulse" 
                        : isDark ? "text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800" : "text-slate-500 hover:text-indigo-700 hover:bg-indigo-50"
                    } disabled:opacity-50`}
                  >
                    <Users className="w-3.5 h-3.5" />
                  </button>
                  <button 
                    onClick={() => setShowProceduresModal(true)} 
                    title="Fichas Salvas"
                    className={`p-1 rounded transition-colors flex items-center justify-center ${
                      isDark ? "text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800" : "text-slate-500 hover:text-indigo-700 hover:bg-indigo-50"
                    }`}
                  >
                    <Database className="w-3.5 h-3.5" />
                  </button>
                  <button 
                    onClick={triggerReloadMockData} 
                    title="Modelo Exemplo"
                    className={`p-1 rounded transition-colors flex items-center justify-center ${
                      isDark ? "text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800" : "text-slate-500 hover:text-indigo-700 hover:bg-indigo-50"
                    }`}
                  >
                    <FileText className="w-3.5 h-3.5" />
                  </button>
                  <button 
                    onClick={triggerResetToBlank} 
                    title="Limpar Tudo"
                    className={`p-1 rounded transition-colors flex items-center justify-center ${
                      isDark ? "text-zinc-400 hover:text-red-400 hover:bg-zinc-800" : "text-slate-500 hover:text-red-600 hover:bg-red-50"
                    }`}
                  >
                    <RotateCcw className="w-3.5 h-3.5" />
                  </button>
                </div>

                {/* Group B: Print Button */}
                <div className={`flex items-center gap-0.5 p-0.5 rounded-md border shadow-xs ${
                   isDark ? "bg-zinc-900/50 border-zinc-800" : "bg-white/50 border-indigo-50/80"
                }`}>
                  <button 
                    onClick={() => setShowPrintModal(true)} 
                    title="Imprimir / PDF"
                    className="p-1 bg-indigo-600 text-white rounded hover:bg-indigo-700 transition-colors flex items-center justify-center active:scale-95"
                  >
                    <Printer className="w-3.5 h-3.5" />
                  </button>
                </div>

                {/* Group C: Settings, Theme & Logout */}
                <div className={`flex items-center gap-0.5 p-0.5 rounded-md border shadow-xs ${
                   isDark ? "bg-zinc-900/50 border-zinc-800" : "bg-white/50 border-indigo-50/80"
                }`}>
                  <button
                    onClick={() => setShowSettingsModal(true)}
                    className={`p-1 rounded transition-colors flex items-center justify-center ${
                      isDark ? "text-zinc-400 hover:bg-zinc-800" : "text-slate-500 hover:bg-indigo-50"
                    }`}
                    title="Configurações"
                  >
                    <Settings className="w-3.5 h-3.5" />
                  </button>
                  <button
                    onClick={toggleTheme}
                    className={`p-1 rounded transition-colors flex items-center justify-center ${
                      isDark ? "text-amber-400 hover:bg-zinc-800" : "text-indigo-600 hover:bg-indigo-50"
                    }`}
                    title="Tema"
                  >
                    {isDark ? <Sun className="w-3.5 h-3.5" /> : <Moon className="w-3.5 h-3.5" />}
                  </button>
                  <button
                    onClick={handleLogout}
                    className={`p-1 rounded transition-colors flex items-center justify-center ${
                      isDark ? "text-rose-400 hover:bg-rose-900/30" : "text-rose-600 hover:bg-rose-50"
                    }`}
                    title="Sair"
                  >
                    <LogOut className="w-3.5 h-3.5" />
                  </button>
                </div>
              </div>
            </div>
          </div>

          {/* Desktop/Tablet Layout (Original beautiful layout, hidden on sm mobile) */}
          <div className="hidden sm:flex flex-row flex-nowrap justify-between items-center gap-2 md:gap-4">
            
            <div className="flex-1 min-w-0 flex items-center gap-2 md:gap-3 overflow-hidden">
              <AnestFlowLogo height={32} className="shrink-0 hidden lg:block" />
              
              <div className={`h-6 w-px shrink-0 hidden lg:block ${isDark ? "bg-zinc-800" : "bg-indigo-100"}`} />
              
              {/* Patient Info Block (Sleeker & Highly Adaptive to prevent occlusion of weight/age) */}
              <div className="flex-1 flex items-center gap-1.5 md:gap-2.5 min-w-0">
                <div className={`relative flex items-center justify-center w-8 h-8 md:w-10 md:h-10 rounded-full border shadow-sm shrink-0 ${
                  isDark ? "bg-zinc-900 border-zinc-700" : "bg-indigo-50/50 border-indigo-100"
                }`}>
                  <FileText className={`w-3.5 h-3.5 md:w-4 md:h-4 ${isDark ? "text-indigo-400" : "text-indigo-600"}`} />
                  <span className={`absolute top-0 right-0 w-2 h-2 md:w-2.5 md:h-2.5 rounded-full border-2 ${isDark ? "border-zinc-900" : "border-white"} ${document.timers.startAnesthesia ? "bg-emerald-500 animate-pulse" : "bg-slate-300"}`}></span>
                </div>
                
                <div className="flex flex-col min-w-0 flex-1">
                  <div className="flex items-center gap-1.5 md:gap-2">
                    <h1 className="text-xs md:text-sm lg:text-base font-bold tracking-tight uppercase line-clamp-1 truncate">
                      {document.patient?.fullName || "Sem Identificação"}
                    </h1>
                    {document.status === "Signed" ? (
                      <span className={`shrink-0 px-1 py-0.5 rounded text-xs md:text-xs font-bold uppercase tracking-widest flex items-center gap-0.5 ${
                        isDark ? "bg-indigo-500/20 text-indigo-300 border border-indigo-500/20" : "bg-indigo-100 text-indigo-700 border border-indigo-200"
                      }`}>
                        <ShieldCheck className="w-2.5 h-2.5 md:w-3 md:h-3" /> <span className="hidden md:inline">Assinado</span><span className="inline md:hidden">Ass</span>
                      </span>
                    ) : (
                      <span className={`shrink-0 px-1 py-0.5 rounded text-xs md:text-xs font-bold uppercase tracking-widest ${
                        isDark ? "bg-amber-500/20 text-amber-300 border border-amber-500/20" : "bg-amber-100 text-amber-700 border border-amber-200"
                      }`}>
                        <span className="hidden md:inline">Rascunho</span><span className="inline md:hidden">Rasc</span>
                      </span>
                    )}
                  </div>
                  <div className={`flex items-center gap-1.5 md:gap-2 text-xs md:text-xs font-semibold leading-none mt-0.5 ${isDark ? "text-zinc-400" : "text-slate-500"}`}>
                    <span className="shrink-0">{document.patient?.age ? `${document.patient?.age}a` : "—"}</span>
                    <span className="shrink-0 w-1 h-1 rounded-full bg-current opacity-30"></span>
                    <span className="shrink-0">{document.patient?.weight ? `${document.patient?.weight}kg` : "—"}</span>
                    <span className="shrink-0 w-1 h-1 rounded-full bg-current opacity-30"></span>
                    <span className="tabular-nums truncate max-w-[70px] sm:max-w-[100px] md:max-w-none">Pront: {document.patient?.recordNumber || "—"}</span>
                  </div>
                </div>
              </div>

              {/* AI Supervisor Active Badge */}
              {aiSupervisorActive && (
                <div className={`hidden lg:flex items-center gap-2 px-3 py-1.5 rounded-xl border animate-pulse shrink-0 ${
                  isDark 
                    ? "bg-amber-500/10 border-amber-500/30 text-amber-400" 
                    : "bg-amber-50 border-amber-200 text-amber-800"
                }`}>
                  <BrainCircuit className="w-4 h-4 animate-spin text-amber-500 shrink-0" style={{ animationDuration: "3s" }} />
                  <div className="flex flex-col text-left text-xs leading-tight">
                    <span className="font-bold uppercase tracking-wider">Supervisor de IA</span>
                    <span className="opacity-85 tabular-nums text-xs truncate max-w-[140px]">
                      {aiSupervisorTask} ({aiSupervisorElapsed}s)
                    </span>
                  </div>
                </div>
              )}
            </div>

            {/* Utility Buttons & Permanent Sync Status */}
            <div className="flex items-center justify-end gap-1.5 md:gap-2 shrink-0">
              
              <SyncStatusBadge
                status={syncEngine.status}
                statusText={syncEngine.statusText}
                isOnline={syncEngine.isOnline}
                pendingCount={syncEngine.pendingCount}
                lastSavedAt={syncEngine.lastSavedAt}
                errorMessage={syncEngine.errorMessage}
                onRetry={syncEngine.retrySyncNow}
                isDark={isDark}
              />

              {/* Time Status */}
              <div className={`flex shrink-0 items-center gap-1 md:gap-2 px-1.5 py-1 md:px-3 md:py-1.5 rounded-lg border shadow-sm ${
                 isDark ? "bg-zinc-900/80 border-zinc-700" : "bg-white/80 border-indigo-100/80"
              }`}>
                 <div className="flex flex-col text-left">
                   <span className="text-xs md:text-xs font-bold uppercase tracking-wider opacity-60 hidden md:inline">Anestesia</span>
                   <span className={`text-xs sm:text-xs md:text-sm font-black tabular-nums leading-none ${isDark ? "text-indigo-400" : "text-indigo-600"}`}>
                     {getElapsedAnesthesiaString()}
                   </span>
                 </div>
                 <div className={`p-1 md:p-1.5 rounded-md ${isDark ? "bg-zinc-800" : "bg-indigo-50"}`}>
                   <Clock className={`w-3.5 h-3.5 ${document.timers.startAnesthesia ? "text-emerald-500 animate-pulse" : "text-indigo-400"}`} />
                 </div>
              </div>

              <div className="w-px h-5 bg-indigo-100 dark:bg-zinc-800 hidden lg:block mx-0.5 shrink-0"></div>

              {/* Action Buttons Group */}
              <div className={`flex shrink-0 items-center gap-0.5 md:gap-1 p-0.5 md:p-1 rounded-lg border shadow-sm ${
                 isDark ? "bg-zinc-900/50 border-zinc-800" : "bg-white/50 border-indigo-50/80"
              }`}>
                <button 
                  onClick={() => setShowShareModal(true)} 
                  disabled={!document.userId} 
                  title={document.userId ? "Gerenciar Equipe e Compartilhamento" : "Salve a ficha primeiro"}
                  className={`p-1.5 md:p-2 rounded-md transition-colors flex items-center justify-center ${
                    syncEngine.status === "syncing" 
                      ? "bg-emerald-100 text-emerald-700 dark:bg-emerald-500/20 dark:text-emerald-300 animate-pulse" 
                      : isDark ? "text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800" : "text-slate-500 hover:text-indigo-700 hover:bg-indigo-50"
                  } disabled:opacity-50 disabled:cursor-not-allowed`}
                >
                  <Users className="w-3.5 h-3.5 md:w-4 md:h-4" />
                </button>
                <button 
                  onClick={() => setShowProceduresModal(true)} 
                  title="Fichas Salvas"
                  className={`p-1.5 md:p-2 rounded-md transition-colors flex items-center justify-center ${
                    isDark ? "text-zinc-400 hover:text-zinc-200 hover:bg-zinc-800" : "text-slate-500 hover:text-indigo-700 hover:bg-indigo-50"
                  }`}
                >
                  <Database className="w-3.5 h-3.5 md:w-4 md:h-4" />
                </button>
                <button 
                  onClick={triggerReloadMockData} 
                  title="Carregar Exemplo Clínico"
                  className="p-1.5 md:p-2 rounded-md transition-colors flex items-center justify-center text-slate-500 hover:text-indigo-700 hover:bg-indigo-50 dark:text-zinc-400 dark:hover:text-zinc-200 dark:hover:bg-zinc-800"
                >
                  <FileText className="w-3.5 h-3.5 md:w-4 md:h-4" />
                </button>
                <button 
                  onClick={triggerResetToBlank} 
                  title="Novo Prontuário"
                  className="p-1.5 md:p-2 rounded-md transition-colors flex items-center justify-center text-slate-500 hover:text-red-600 hover:bg-red-50 dark:text-zinc-400 dark:hover:text-red-400 dark:hover:bg-zinc-800"
                >
                  <RotateCcw className="w-3.5 h-3.5 md:w-4 md:h-4" />
                </button>
              </div>
              
              {/* Compact Print Button */}
              <button 
                onClick={() => setShowPrintModal(true)} 
                className="shrink-0 px-2 py-1.5 md:px-3 md:py-2 bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl shadow-md font-bold text-xs md:text-xs flex items-center justify-center gap-1 md:gap-1.5 transition-all active:scale-95 ml-0.5 md:ml-1"
                title="Imprimir"
              >
                <Printer className="w-3.5 h-3.5 md:w-4 md:h-4"/>
                <span className="hidden md:inline">Imprimir</span>
              </button>

              <div className="w-px h-5 bg-indigo-100 dark:bg-zinc-800 hidden lg:block mx-0.5 shrink-0"></div>

              {/* Preferences & Exit */}
              <div className={`flex shrink-0 items-center gap-0.5 md:gap-1 p-0.5 md:p-1 rounded-lg border shadow-sm ${
                 isDark ? "bg-zinc-900/50 border-zinc-800" : "bg-white/50 border-indigo-50/80"
              }`}>
                <button
                  onClick={() => setShowSettingsModal(true)}
                  className={`p-1.5 md:p-2 rounded-md transition-colors flex items-center justify-center ${
                    isDark ? "text-zinc-400 hover:bg-zinc-800" : "text-slate-500 hover:bg-indigo-50"
                  }`}
                  title="Configurações"
                >
                  <Settings className="w-3.5 h-3.5 md:w-4 md:h-4" />
                </button>

                <button
                  onClick={toggleTheme}
                  className={`p-1.5 md:p-2 rounded-md transition-colors flex items-center justify-center ${
                    isDark ? "text-amber-400 hover:bg-zinc-800" : "text-indigo-600 hover:bg-indigo-50"
                  }`}
                  title={isDark ? "Mudar para Modo Claro" : "Mudar para Modo Escuro"}
                >
                  {isDark ? <Sun className="w-3.5 h-3.5 md:w-4 md:h-4" /> : <Moon className="w-3.5 h-3.5 md:w-4 md:h-4" />}
                </button>

                <button
                  onClick={handleLogout}
                  className={`p-1.5 md:p-2 rounded-md transition-colors flex items-center justify-center ${
                    isDark ? "text-rose-400 hover:bg-rose-900/30 hover:text-rose-300" : "text-rose-600 hover:bg-rose-50"
                  }`}
                  title="Sair do Portal Médico"
                >
                  <LogOut className="w-3.5 h-3.5 md:w-4 md:h-4" />
                </button>
              </div>

            </div>
          </div>
        </div>
      </header>
